<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <div class="title">
      <h1>Owner Details</h1>
    </div>

    <div class="owner_details_container">

      <p>Name: <%= @owner.name %></p>
      <p>Email: <%= @owner.email %></p>
      <p>Phone: <%= @owner.phone %></p>
      <p>Bio: <%= @owner.bio %> </p>
      <p>Has cats? <strong><%= @owner.has_cats == 't'? "Yes" : "No" %></strong></p>
      <p>Has other dogs? <strong><%= @owner.has_other_dogs == 't'? "Yes" : "No" %></strong></p>
      <p>Has children? <strong><%= @owner.has_children == 't'? "Yes" : "No" %></strong></p>

      <h3> Dogs reserved for <%= @owner.name %> : </h3>

        <p><% @dogs.each do |dog| %>
          <% if dog.owner_id == @owner.id %>
              <ul><%= dog.name %></ul>
          <% end %>

        <% end %> </p>

      <h3><a href="/owners/<%= @owner.id %>/edit">Edit Owner Details</a></h3>
      <h3> Possible matches below:</h3>
        <% names = @matches.map { |match|  match.name } %>
          <%names.each do |name| %>
          <ul><%= name %></ul>
        <% end %>
      <hr>
      <form action="/owners/<%= @owner.id %>/delete" method="POST"><input type="submit" value="Delete this owner"></form>

    </div>
  </body>
</html>
